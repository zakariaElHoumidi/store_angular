import { PopularProductPipe } from './popular-product.pipe';

describe('PopularProductPipe', () => {
  it('create an instance', () => {
    const pipe = new PopularProductPipe();
    expect(pipe).toBeTruthy();
  });
});
